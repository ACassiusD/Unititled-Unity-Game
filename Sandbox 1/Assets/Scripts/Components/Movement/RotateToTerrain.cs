using UnityEngine;

public class RotateToTerrain : MonoBehaviour
{
    //public void Update()
    //{
    //    Vector3 direction;
    //    float distance = 50;

    //    direction = new Vector3(0, -1, 0);

    //    Vector3 origin = this.transform.position;
    //    bool isObjectHit = false;
    //    RaycastHit hitinfo = new RaycastHit();

    //    Debug.DrawRay(transform.position, direction * distance, Color.green);
    //    //Check if the ray hit something, store it in hitinfo
    //    if (Physics.Raycast(origin, direction, out hitinfo, distance, 1 << 9))
    //    {
    //        var normal = hitinfo.normal;
    //        Transform hitObject = hitinfo.transform;
    //        Debug.Log(hitinfo.normal);
    //        isObjectHit = true;

    //    }
    //    else
    //    {
    //        isObjectHit = false;
    //    }

    //}
}
